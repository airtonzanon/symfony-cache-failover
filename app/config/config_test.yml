imports:
    - { resource: config_dev.yml }

framework:
    test: ~
    session:
        storage_id: session.storage.mock_file
    profiler:
        collect: false

parameters:
    database_name: easytaxi-symfony-cache-test
    cache_prefix: test